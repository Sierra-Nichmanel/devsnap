import EmptyState from "@/components/common/EmptyState";

const Collections = () => {
  return (
    <EmptyState
      title="No Collections"
      description="Create your first API collection."
    />
  );
};

export default Collections;
